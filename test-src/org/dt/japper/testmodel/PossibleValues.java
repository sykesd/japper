package org.dt.japper.testmodel;

/**
 * An enum we can use as the type of a field in a model class to make sure our
 * property matching code does not blow up when faced with such a property.
 */
public enum PossibleValues {
    ONE,
    TWO,
    THREE,
    ;
}
